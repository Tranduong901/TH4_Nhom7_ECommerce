name: th4_nhom7_ecommerce
description: "TH4 - Mini E-Commerce App for Group 7"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4

  # State Management
  provider: ^6.1.2
  
  # Network & UI
  http: ^1.1.0
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  carousel_slider: ^5.0.0
  badges: ^3.1.2
  image_picker: ^1.1.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
